var path = require('path');
var webpack = require('webpack');

module.exports = {
    entry: {
        'main': './src/main.js',
    },
    output: {
        path: path.resolve(__dirname, 'dist'),
        library: '',
        libraryTarget: 'umd',
        filename: 'vnilla.js'
    },
    module: {
        rules: [
            {
                test: /\.js?$/,
                use: {
                    loader: 'babel-loader',
                    options: {
                        presets: ['es2015']
                    }
                }
            }
        ]
    },
    stats: {
        colors: true
    },
    mode: 'development', // for now -- later should find how to switch between dev/prod
};
